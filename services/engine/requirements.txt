fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0

# Supabase / database
# postgrest is a transitive dependency of supabase — do not pin it separately.
# Explicit pins cause httpx version triangulation failures because postgrest 0.19.x
# caps at httpx<0.28 while newer pins may want 0.28+.
supabase==2.11.0

# HTTP — must stay <0.28 while supabase 2.11.0 pulls in postgrest 0.19.x
httpx==0.27.2
python-multipart==0.0.20

# JWT validation
python-jose[cryptography]==3.3.0

# Financial math
numpy==2.2.1
scipy==1.15.0
pandas==2.2.3

# Utilities
python-dateutil==2.9.0.post0

# Observability
sentry-sdk[fastapi]==2.22.0

# Redis — shared cross-worker cache for benchmark data and rate-limit counters.
# hiredis is a C extension that accelerates response parsing (~5-10x).
# Optional: the app falls back to in-process state if REDIS_URL is not set.
redis[hiredis]==5.2.1
